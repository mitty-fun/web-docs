---
title: '四則運算'
---
import Playground  from '../../../src/components/playground'


## 程式運作規則
* 先乘除、後加減、小括號先做
* 數字相互計算只要有任何一個是浮點數結果就一定是浮點數
* 數字相除結果一定是浮點數
* 加號可以做數學相加、字串串接、串列串接
* 乘號可以做數學相乘、字串複製串接、串列複製串接

### 加號
不同的資料型態相加會有不同的作用
* 數字 + 數字，是數學運算
* 字串 + 字串，是字串串接
* 串列 + 串列，是串列串接

<Playground python={`
print(10 + 20 + 30) # 60
print('你好' + '王小明' + '!') # 你好王小明！
print(['a', 'b', 'c'] + ['d', 'e']) # ['a', 'b', 'c', 'd', 'e']
`}/>

### 乘號
不同的資料型態相乘會有不同的作用
* 數字 * 數字，是數學運算
* 字串 * 數字，是字串複製並串接
* 串列 * 數字，是串列複製並串接

<Playground python={`
print(5 * 10) # 50
print('Hi' * 10) # HiHiHiHiHiHiHiHiHiHi
print(['a', 'b', 'c'] * 3) # ['a', 'b', 'c', 'a', 'b', 'c', 'a', 'b', 'c']
`}/>

### 浮點數
不同的資料型態相乘會有不同的作用
* 只要做除法結果一定是浮點數
* 計算的兩邊只要任何一個是浮點數，結果一定是浮點數

<Playground python={`
print(10 + 10) # 20
print(10 - 10) # 0
print(10 * 10) # 100
print(10 / 10) # 1.0 ---> 浮點數\n
print(10 + 5.0) # 15.0
print(10 - 5.0) # 5.0
print(10.0 * 5) # 50.0
print(10.0 / 5) # 2.0
`}/>

### 次方

a = 2
b = 3
c = 5
<Playground python={`
print(a ** b) # 8
print(a ** c) # 32\n
print(b ** a) # 9
print(b ** c) # 243\n
print(c ** a) # 25
print(c ** b) # 125
`}/>