---
title: '經典 35 道考題 1-10'
sidebar_position: 1
---

import Quiz  from '../../../src/components/quiz'

## 1 資料型態
```python
a = 10 / 3
b = 10 % 3
c = 10 // 3

print(a, b, c)
```
<Quiz
  description={`請問執行以下程式最後會印出什麼？`}
  options={[
    '3.3333333333333335, 1, 3',
    '3, 1, 3.3333333333333335',
    '3, 3.3333333333333335, 1',
    '3.3333333333333335, 1, 3.0',
  ]}
  answer={0}
  explanation={"百分比 % 符號是取餘數，兩個斜線 // 是整除法"}
/>

## 2 資料型態
```python
a = 10 > 10
b = 10 < 10
c = 10 == 10
d = a and b and c
e = a or b or c
f = a == b
g = a == c

print(a, b, c)
```
<Quiz
  description={`請問執行以下程式後變數 a - g 中的布林值資料有多少個 True？`}
  options={[
    '2',
    '3',
    '4',
    '5',
  ]}
  answer={1}
  explanation='a: False、b: False、c: True、d: False、e: True、f: True、g: False'
/>

## 3 資料型態
```python
a = input("矩形長？")
b = input("矩形高？")

print("面積是：" + area)
```
<Quiz
  description={`小明遇到了「困難」，他希望寫一隻程式能讓使用者輸入矩形長和高兩個數值，並計算出面積並印出。請你完成第 3 行並幫助小明完成這隻程式？`}
  options={[
    'area = str(int(a) * int(b))',
    'area = int(a) * int(b)',
    'area = str(a * b)',
    'area = int(a * b)',
  ]}
  answer={0}
  explanation='因為 input 取得的輸入資料型態是字串，因此計算錢要先用 int 轉為數值型態計算，最後再使用 str 將結果轉回字串才能再印出時做串接的動作。'
/>

## 4 資料型態
```python
type(5.0)
type("True")
type(False)
```
<Quiz
  description={`請問執行 type 去檢查以下四個資料的型態分別會得到？`}
  options={[
    'int, bool, bool',
    'float, str, bool',
    'int, str, bool',
    'int, str, str',
  ]}
  answer={1}
  explanation='數值帶有小數點一定就是浮點數 float 資料型態'
/>

## 5 資料型態
```python
a = 10
b = "10"

print(type(a + a))
print(type(b + b))
print(type(a / a))
print(type(a * b))
```
<Quiz
  description={`請問分別會印出什麼型態？`}
  options={[
    'int, str, int, str',
    'int, str, float, str',
    'int, str, float, float',
    'int, int, float, str',
  ]}
  answer={1}
  explanation={`
    a + a 得到 20 資料型態是整數
    b + b 得到 1010 資料型態是字串
    a / a 得到 1.0 資料型態是浮點數
    a * b 得到 10101010101010101010 資料型態是字串
    a + a 得到 20 資料型態是整數
  `}
/>


## 6 變數
```python
a = 10
b = 20
c = a + b
d = b + c
e = -10
b = b - a
e = b - a

print(a + b + c + d + e)
```
<Quiz
  description={`請問執行以下程式最後會印出什麼？`}
  options={[
    '100',
    '110',
    '120',
    '130',
  ]}
  answer={0}
  explanation={`a、b、c、d、e 分別是 10、10、30、50、0 總和後為 100`}
/>

## 7 變數
```python
a = 10
b = 20
c = 30
a = a + b + c
b = a + b + c
c = a + b + c
```
<Quiz
  description={`請問執行以下程式最後變數 a, b, c 分別是多少？`}
  options={[
    '60, 60, 60',
    '60, 80, 170',
    '60, 110, 200',
    '60, 80, 140',
  ]}
  answer={2}
  explanation={`a: 60、b: 110、c: 200`}
/>


## 8 四則運算
```python
ch = int(input('國文：'))
en = int(input('英文：'))
ma = int(input('數學：'))

avg = ch + en + ma / 3

print(avg)
```
<Quiz
  description={`執行以下程式，並分別輸入三個成績 70, 80, 90 請問會印出什麼？`}
  options={[
    '180',
    '80',
    '250',
    '程式錯誤',
  ]}
  answer={0}
  explanation={`四則運算的規則是先乘除後加減，小括號優先做，因此程式會先執行除法再執行相加。`}
/>

## 9 四則運算
```python
print((1 + 2) * (3 - 4) / (5 % 6))
```
<Quiz
  description={`執行以下程式最後會印出什麼？`}
  options={[
    '0.6',
    '-0.6',
    '6',
    '-6',
  ]}
  answer={1}
  explanation={`
    Step0 (1 + 2) * (3 - 4) / (5 % 6)
    Step1 3 * -1 / 5
    Step2 -3 / 5
    Step3 -0.6
  `}
/>

## 10 四則運算
```python
'10' + '10'
'10' - '10'
'10' * 5
'10' / 4
```
<Quiz
  description={`請問以下四行的程式中有幾行的計算會造成錯誤？`}
  options={[
    '1',
    '2',
    '3',
    '4',
  ]}
  answer={1}
  explanation={`字串與字串之間不能相減 & 字串不能做除法`}
/>