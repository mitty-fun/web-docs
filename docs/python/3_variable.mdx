---
title: '3. 認識變數'
sidebar_position: 3
---
import Playground  from '../../src/components/playground'

# 認識變數
幾乎所有的程式語言都會設計變數，變數是程式用來記住事情的方式。

## 變數是什麼？
變數就像是一個盒子，可以用來儲存資料！
這個盒子要貼上一個標籤，這樣當我們有很多個盒子的時候，就可以透過標籤去知道這裡面裝了什麼

## 變數的命名限制
* 只能大小寫字母、數字、底線、中文字
* 變數名稱第一個字不能是數字
* 大小寫視為不同變數，例如 Apple 跟 apple 就是不一樣的變數名稱
* 不能跟程式語法衝突，例如：if、while

## 等號賦值
程式中的等號跟數學的等號是完全不一樣的東西
等號有「賦值」的意思，程式中的等號會把等號右邊的內容，存進左邊的對象（通常是變數）
例如：a = 10 就是把資料數字 10 存進變數 a 裡面

程式中 `=` 等號語法是告訴電腦，將等號右邊的資料存入左邊的變數 a 中。

試著加入第三行 `print(a)` 程式並執行看看會印出什麼吧。
<Playground python={`a = 10`} />

我們很經常要創造變數的同時，將預設的資料存入，因此我們可以合併成一行：
<Playground python={`a = '嗨';\nprint(a);`} />

## 修改變數
執行以下範例看看最後會印出什麼？
<Playground python={`
a = 10 #創造變數 a 並存入資料 10
print(a) #印出 10
a = 20 #設定變數 a 的資料為 20
print(a) #印出 20
a = 30 #設定變數 a 的資料為 30
print(a) #印出 30
`}/>

## 挑戰你的大腦
創造兩個變數 a 和 b 並都分別存入數值 10 和 20，想想看最後 a 和 b 是存入什麼樣的內容？<br/>
試著加入 `print(a, b)` 到最後一行並執行看看吧
<Playground python={`
a = 10
b = 20
a = b
b = a
`} />

創造兩個變數 a 和 b 並都分別存入數值 10，接著做了兩行的的相加計算，試著想想看最後 a 和 b 會存入什麼樣的內容？<br/>
試著加入 `print(a, b)` 到最後一行並執行看看吧
<Playground python={`
a = 10
b = 10
a = a + b
b = a + b
`} />
